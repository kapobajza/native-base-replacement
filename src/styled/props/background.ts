import type { StyledBackgroundKey, SystemConfig } from '../types';

export const background: SystemConfig<StyledBackgroundKey> = {
  backgroundSize: true,
  backgroundPosition: true,
  backgroundRepeat: true,
  backgroundAttachment: true,
  backgroundBlendMode: true,
};
